<%= link_to "New picture",
            new_picture_path,
            :class => 'btn btn-primary' %>
<br />
<br />
<div class="row">
  <% @pictures.each do |picture| %>
    <div class="col-sm-6 col-md-6">
      <div class="thumbnail">
        <h3 ><%= picture.title %> <small>(<%= picture.added %>)</small></h3>
        <%= image_tag(picture.image.thumb)%>
        <div class="caption">
          
        <br />
        <br />
        <div class="form-group">
          <div class="col-lg-12 col-lg-offset-2">
            <div class="btn-group">
              <% if can? :edit, picture %>
                <%= link_to t('.edit', :default => t("helpers.links.edit")),
                edit_picture_path(picture), :class => 'btn btn-mini btn-danger' %>  
              <% end %>
              <% if can? :destroy, picture %>
                <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                picture_path(picture),
                :method => 'delete',
                :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                :class => 'btn btn-primary' %>
              <% end %>
<<<<<<< HEAD
              <% if can? :createmem, picture %>
                <%= link_to "Create mem",
                        createmem_picture_path(picture), :class => 'btn btn-mini btn-info' %>
=======
              <% if can? :creategem, picture %>
                <%= link_to "Create gem",
                        creategem_picture_path(picture), :class => 'btn btn-mini btn-info' %>
>>>>>>> 349c17d716b988f25ddac9790b33e1f40a006eb4
              <% end %>
            </div>
          </div>
          </div>
        </div>
      </div>
    </div>
  <% end %>
</div>






