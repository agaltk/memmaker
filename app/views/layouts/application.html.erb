<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Memmaker" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

  <%= stylesheet_link_tag    "application" %>
  <%= javascript_include_tag "application" %>
<%= stylesheet_link_tag "amelia", :media => "all" %>

    <%= yield(:page_stylesheet) if content_for?(:page_stylesheet) %>

  </head>
  <body>

   

    <!-- Fixed navbar -->
    <div class="navbar navbar-inverse navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <a class="navbar-brand" href="#">Mem Maker</a>
        </div>
        <div class="navbar-collapse collapse">
          <ul class="nav navbar-nav">
            <li class="<%= 'active' if current_page?(show_mems_path(:type => 'all')) %>">
               <%= link_to 'Home', show_mems_path(:type => 'all') %></a>
            </li>
            <% if user_signed_in?%>
              <li class="<%= 'active' if current_page?(pictures_path) %>">
               <%= link_to 'All pictures', pictures_path %>
              </li>
              <li class="<%= 'active' if current_page?(show_pictures_path(:type => 'my')) %>">
               <%= link_to 'My pictures', show_pictures_path(:type => 'my') %>
              </li>
              <li class="<%= 'active' if current_page?(show_mems_path(:type => 'my')) %>">
               <%= link_to 'My mems', show_mems_path(:type => 'my')%>
              </li>
              <li class="<%= 'active' if current_page?(destroy_user_session_path) %>">
               <%= link_to "Wyloguj", destroy_user_session_path, :method => :delete, :class=>"small button"%>
              </li>
              <li class="<%= 'active' if current_page?(edit_user_registration_path) %>">
               <%= link_to "Edytuj", edit_user_registration_path, :class=>"small button"%>
             </li>
            <% else %>
            <li class="<%= 'active' if current_page?(new_user_registration_path) %>">
              <%= link_to "Zarejestruj", new_user_registration_path, :class=>"small button"%>
            </li>
            <li class="<%= 'active' if current_page?(new_user_session_path) %>">
               <%= link_to "Zaloguj", new_user_session_path, :class=>"small button"%>
             </li>
            <% end %> 
          </ul>
        </div><!--/.nav-collapse -->
      </div>
    </div>

    <div class="container theme-showcase">

      <div class="jumbotron">
        <h1>Welcome into memmaker :)</h1>
  
      </div>

      <%= yield %>

      


     











      

    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "amelia" %>
    <%= yield(:page_javascript) if content_for?(:page_javascript) %>
    

  </body>
  
</html>
